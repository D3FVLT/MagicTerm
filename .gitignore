# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
out/
*.tsbuildinfo

# But keep electron-builder resources
!apps/desktop/build/

# Electron
release/

# Environment
.env
.env.local
.env.*.local

# Allow committing the web app's .env — it only holds the public Supabase URL
# and anon key, which are safe to expose (RLS is what guards the data).
!apps/web/.env

# IDE
.idea/
*.swp
*.swo
.vscode/*
!.vscode/extensions.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Turbo
.turbo/

# Astro (web app)
.astro/
apps/web/dist/

# Testing
coverage/
apps/desktop/test/test-dist/

# Local Remotion / agent tooling — not part of the product
# (promo video project, installed Remotion agent skills, skills lockfile)
tools/promo-video/
.agents/
skills-lock.json

# Local agent checklist for shipping a release. Stays on this machine.
.cursor/rules/release.mdc
