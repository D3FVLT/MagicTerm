<section id="security" class="security">
  <h2>Before you install</h2>
  <ul>
    <li>Encrypted on this device. The master password is not uploaded.</li>
    <li>Local mode needs no account. Sync is optional.</li>
    <li>Builds are not signed.</li>
    <li>SHA256 for each installer is on the <a href="/download">download page</a>.</li>
  </ul>
</section>

<style>
  .security {
    max-width: 1080px;
    margin: 0 auto;
    padding: 64px 24px 0;
  }

  h2 {
    margin: 0 0 12px;
    color: var(--fg);
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  li {
    padding: 10px 0;
    border-top: 1px solid var(--border);
    color: var(--fg);
    font-size: 15px;
    line-height: 1.45;
  }

  li:last-child {
    border-bottom: 1px solid var(--border);
  }

  a {
    color: var(--accent);
    text-decoration: none;
  }

  a:hover {
    color: var(--accent-hover);
  }
</style>
